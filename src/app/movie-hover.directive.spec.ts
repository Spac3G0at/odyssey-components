import { MovieHoverDirective } from './movie-hover.directive';

describe('MovieHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieHoverDirective();
    expect(directive).toBeTruthy();
  });
});
